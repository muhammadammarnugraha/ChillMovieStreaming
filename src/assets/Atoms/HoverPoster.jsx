import React from "react";

const HoverPoster = ({
  className,
  daftarFilm,
  sizeCard,
  cardForm,
  moveLeft,
  moveRight,
  isHorizontal,
  cek
}) => {
  return <div>{cek}</div>;
};

export default HoverPoster;
